#name: CI/CD Pipeline
#
#on:
#    push:
#        branches: [ main ]
#    pull_request:
#        branches: [ main ]
#
#jobs:
#    # Validate code quality and structure
#    validate:
#        name: ✅ Validate
#        runs-on: ubuntu-latest
#
#        steps:
#            - name: Checkout code
#              uses: actions/checkout@v4
#
#            - name: Check required files
#              run: |
#                  echo "Checking essential files..."
#                  test -f index.html || { echo "❌ Missing index.html"; exit 1; }
#                  test -f manifest.json || { echo "❌ Missing manifest.json"; exit 1; }
#                  test -f sw.js || { echo "❌ Missing service worker"; exit 1; }
#                  test -f package.json || { echo "❌ Missing package.json"; exit 1; }
#                  echo "✅ All required files present"
#
#            - name: Validate JSON files
#              run: |
#                  echo "Validating JSON syntax..."
#                  python3 -m json.tool manifest.json > /dev/null
#                  python3 -m json.tool package.json > /dev/null
#                  echo "✅ JSON files are valid"
#
#    # Test the application works
#    test:
#        name: 🧪 Test
#        runs-on: ubuntu-latest
#        needs: validate
#
#        steps:
#            - name: Checkout code
#              uses: actions/checkout@v4
#
#            - name: Start server and test
#              run: |
#                  echo "Starting local server..."
#                  python3 -m http.server 8000 &
#                  sleep 2
#
#                  echo "Testing application..."
#                  curl -f http://localhost:8000/ | grep -q "Calendar Planner" || { echo "❌ App failed to load"; exit 1; }
#                  curl -f http://localhost:8000/manifest.json > /dev/null || { echo "❌ Manifest failed to load"; exit 1; }
#                  curl -f http://localhost:8000/sw.js > /dev/null || { echo "❌ Service worker failed to load"; exit 1; }
#
#                  echo "✅ Application tests passed"
#
#    # Deploy to GitHub Pages
#    deploy:
#        name: 🚀 Deploy
#        runs-on: ubuntu-latest
#        needs: [validate, test]
#        # Legacy deploy is disabled by default to avoid conflict with pages-preview.yml
#        # Re-enable by setting repository variable USE_LEGACY_PAGES to 'true'
#        if: github.ref == 'refs/heads/main' && github.event_name == 'push' && vars.USE_LEGACY_PAGES == 'true'
#
#        permissions:
#            contents: read
#            pages: write
#            id-token: write
#
#        environment:
#            name: github-pages
#            url: ${{ steps.deployment.outputs.page_url }}
#
#        steps:
#            - name: Checkout code
#              uses: actions/checkout@v4
#
#            - name: Setup Pages
#              uses: actions/configure-pages@v4
#
#            - name: Upload to Pages
#              uses: actions/upload-pages-artifact@v3
#              with:
#                  path: '.'
#
#            - name: Deploy to GitHub Pages
#              id: deployment
#              uses: actions/deploy-pages@v4
#
#            - name: Success message
#              run: |
#                  echo "🎉 Deployed successfully!"
#                  echo "🌐 Live at: ${{ steps.deployment.outputs.page_url }}"
#
#                  # Add GitHub Step Summary
#                  echo "## 🚀 Deployment Summary" >> $GITHUB_STEP_SUMMARY
#                  echo "- **Status**: ✅ Success" >> $GITHUB_STEP_SUMMARY
#                  echo "- **Environment**: GitHub Pages" >> $GITHUB_STEP_SUMMARY
#                  echo "- **URL**: ${{ steps.deployment.outputs.page_url }}" >> $GITHUB_STEP_SUMMARY
#                  echo "- **Branch**: main" >> $GITHUB_STEP_SUMMARY
#                  echo "- **Commit**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
#                  echo "- **Time**: $(date)" >> $GITHUB_STEP_SUMMARY
